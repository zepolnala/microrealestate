// apps/landing/next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'cdn.investorpro.com'],
  },
  // Quitar la sección experimental que está deprecated
}

module.exports = nextConfig
